# Ultra-minimal requirements for Render Free Tier (512MB RAM)
# Removes heavy ML dependencies to fit in memory limit

# Core Web Framework
Flask==3.0.0
Werkzeug==3.0.1
python-dotenv==1.0.0
gunicorn==21.2.0

# PDF Processing (lightweight)
pypdfium2==4.26.0
PyMuPDF==1.24.0

# Document Processing
python-docx==1.1.0
Pillow==10.0.0

# LLM Provider (only Groq - lightest)
groq==1.4.0

# Database & Storage
supabase==2.28.0
httpx==0.26.0

# Utilities
requests==2.31.0
phonenumbers==8.13.26
